// RETIRED — do not run.
// The HTML pages in this repo are hand-maintained. This generator is no longer part of
// the build/deploy path (deploy.yml only runs `wrangler deploy`), and running it would
// overwrite hand-edited SEO/OG tags, analytics, and Greek localization.
// To remove it from the repo entirely:  git rm scripts/build-language-pages.mjs
console.error("build-language-pages.mjs is retired — pages are hand-maintained. Aborting without changes.");
process.exit(1);
